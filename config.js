// Generations runs fully local-first (browser storage + IndexedDB) — no config needed.
// This placeholder is reserved for optional cloud sync (Supabase) in a future version.
// Leaving it blank keeps the app 100% local and offline-capable.
window.GENERATIONS_CONFIG = {
  supabaseUrl: "",
  supabaseAnonKey: ""
};
